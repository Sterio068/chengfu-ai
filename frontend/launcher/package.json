{
  "name": "chengfu-launcher",
  "version": "1.3.0",
  "description": "AI Launcher · esbuild bundle for prod",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.config.js",
    "build:watch": "node build.config.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
