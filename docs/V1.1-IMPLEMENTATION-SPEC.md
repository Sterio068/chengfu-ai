# 承富 AI v1.1 · 4 個閉環實作規格

> **來源:** Round 8 外部 reviewer 完整技術規格 + 工時估
> **狀態:** 等承富老闆答 5 題後動工(見 `docs/QUESTIONS-FOR-OWNER.md`)
> **執行順序(reviewer 推薦):** B(8-12h) → A(6-8h) → D(6-8h) → C(6-8h · v1.2)
> **總工時:** P1 兩週內 20-28h(B+A+D)· P2 v1.1 補 12-18h(C 與 A/B 進階)

---

## A · Fal.ai Recraft v3 真生圖

### 0. 現狀
- ✅ Fal action schema 已定義 Recraft v3 的 `prompt / image_size / style / num_images`
  位於 `config-templates/actions/fal-ai-image-gen.json:14`
- ✅ 後端外部 API 呼叫 pattern 已有(`backend/accounting/orchestrator.py:21` 用 `httpx.AsyncClient`)
- ❌ 缺後端 endpoint + Launcher UI 選尺寸/重生/失敗提示

### 1. API 設計

```
POST /api-accounting/design/recraft

Request:
{
  "project_id": "65xxx?",      # 可選 · 串到 project metadata
  "prompt": "中秋節品牌主視覺...",
  "image_size": "square_hd",    # square_hd | portrait_16_9
  "style": "realistic_image",   # 預設
  "regenerate_of": "req_xxx?"   # 重生:換 seed 但保留參數
}

Response (success):
{
  "job_id": "req_xxx",
  "status": "done",
  "images": [{"url": "...", "width": 1024, "height": 1024}],
  "friendly_message": null
}

Response (pending · 仍在排隊):
{
  "job_id": "req_xxx",
  "status": "pending",
  "friendly_message": "生圖中,請稍候(目前 Fal 排隊)"
}

Response (moderation 拒絕):
{
  "status": "rejected",
  "friendly_message": "描述太像真人、官方標誌或敏感文字,請改成抽象視覺描述"
}
```

### 2. 設計決策(reviewer 建議)
- **預設 1 張 · 不要 3 張** — 政府/老品牌案是「慢慢收斂」不是「海選」 · 1 張 + 重生按鈕成本與選擇疲勞最低
- **Fal 不寫 Mongo · 只記 design_jobs log** — 圖檔留在 Fal CDN
- **failure path 用人話** — 不暴露 Fal 原文錯誤訊息

### 3. Python 實作示意

```python
# backend/accounting/main.py(新增 endpoint)
import httpx, asyncio

FAL_QUEUE_URL = "https://queue.fal.run/fal-ai/recraft-v3"
FAL_KEY = os.getenv("FAL_API_KEY", "")

class RecraftRequest(BaseModel):
    project_id: Optional[str] = None
    prompt: str
    image_size: Literal["square_hd", "portrait_16_9"] = "square_hd"
    style: str = "realistic_image"
    regenerate_of: Optional[str] = None

@app.post("/design/recraft")
async def design_recraft(req: RecraftRequest, email: Optional[str] = Depends(current_user_email)):
    if not FAL_KEY:
        raise HTTPException(503, "Fal.ai 未設定 (FAL_API_KEY 缺)")
    headers = {"Authorization": f"Key {FAL_KEY}", "Content-Type": "application/json"}
    payload = {
        "prompt": req.prompt,
        "image_size": req.image_size,
        "style": req.style,
        "num_images": 1,
    }
    async with httpx.AsyncClient(timeout=15) as c:
        try:
            q = await c.post(FAL_QUEUE_URL, headers=headers, json=payload)
            q.raise_for_status()
            req_id = q.json()["request_id"]
        except httpx.HTTPStatusError as e:
            if e.response.status_code in (400, 422):
                return {"status": "rejected",
                        "friendly_message": "描述太像真人、官方標誌或敏感文字,請改成抽象視覺描述"}
            raise HTTPException(502, "Fal.ai 服務忙碌中")
        # Poll up to 12 秒
        for _ in range(12):
            s = await c.get(f"{FAL_QUEUE_URL}/requests/{req_id}/status", headers=headers)
            if s.json().get("status") == "COMPLETED":
                r = await c.get(f"{FAL_QUEUE_URL}/requests/{req_id}", headers=headers)
                # 記 log
                db.design_jobs.insert_one({
                    "request_id": req_id, "user": email, "project_id": req.project_id,
                    "prompt": req.prompt, "size": req.image_size,
                    "created_at": datetime.utcnow(),
                })
                return {"job_id": req_id, "status": "done",
                        "images": r.json().get("images", []), "friendly_message": None}
            await asyncio.sleep(1)
        return {"job_id": req_id, "status": "pending",
                "friendly_message": "生圖中,請稍候,可關掉視窗稍後查看歷史"}
```

### 4. 失敗 / 降級
| 情境 | 處理 |
|---|---|
| `FAL_API_KEY` 未設 | 503 + 「Fal.ai 未設定」 |
| 400/422 moderation | rejected + 人話訊息 |
| 5xx / timeout | 502 + 「服務忙碌中,請稍後重生」 |
| 12 秒未完成 | pending + job_id(前端可後續 poll) |

### 5. 前端 UI(`modules/chat.js` 設計助手特化)

設計助手對話框出現 prompt 後,助手回覆下方加 3 個按鈕:
- 📐 換尺寸(square_hd ↔ portrait_16_9)
- 🔄 重生一張(regenerate_of=last_job_id)
- 💾 存到專案(若當前在 project context)

### 6. 測試 / 驗收
- [ ] `pytest test_design.py` mock httpx · 驗成功 / moderation / timeout 三路
- [ ] ⌘3 進入設計助手 · 能選尺寸生 1 張 · 按重生換 seed · 保留上一張結果
- [ ] 失敗訊息**沒有**任何英文 stack trace 或 `request_id` 給使用者看

### 7. 風險
- moderation 卡點:真人肖像 / 政府機關 logo / 把繁中標語精準排進圖
- → UI 文案教育同仁:**抽象視覺描述 vs 寫實要求**

### 8. 工時:**6-8h**

---

## B · PDF 文字抽取 MVP(老闆 top 2 · 70 頁複製貼上痛點)

### 0. 現狀
- ✅ 輸入框「📄 長文件 3 步貼法」hover 提示已上(過渡方案)
- ❌ 缺真實檔案入口 + 後端抽字 endpoint
- 不要動現有 chat 純 JSON SSE flow(`chat.js:209`)

### 1. API 設計

```
POST /api-accounting/pdf/extract
Content-Type: multipart/form-data
Body: file=<pdf binary>

Response:
{
  "stats": {
    "page_count": 58,
    "ocr_pages": 4,
    "extract_seconds": 3.2
  },
  "pages": [
    {"page": 1, "text": "招標公告...", "ocr_used": false, "char_count": 812},
    {"page": 2, "text": "壹、計畫概述...", "ocr_used": false, "char_count": 945},
    ...
  ],
  "combined_text": "=== 第 1 頁 ===\n招標公告...\n\n=== 第 2 頁 ===\n壹、計畫概述..."
}
```

### 2. 設計決策(reviewer 建議)
- **獨立 endpoint · 不混進 `/api/ask/agents`** — 可獨立測 / 可重用給投標顧問 / NAS / RAG
- **同時回 pages + combined_text** — pages 給未來頁碼引用 / combined_text 給前端直接塞 textarea
- **PyMuPDF first / OCR fallback** — 不一開始就 OCR(政府 PDF 多 born-digital · OCR 慢且錯字)
- **不選 pdfplumber 當 base** — 它對表格強但中文公文穩定度遜 PyMuPDF

### 3. Python 實作示意

```python
# backend/accounting/main.py(新增 endpoint · 需 pip install pymupdf)
import fitz  # pymupdf
from fastapi import UploadFile, File

@app.post("/pdf/extract")
async def pdf_extract(file: UploadFile = File(...)):
    if file.content_type not in ("application/pdf",):
        raise HTTPException(400, "請上傳 PDF 檔案")
    pdf_bytes = await file.read()
    if len(pdf_bytes) > 30 * 1024 * 1024:
        raise HTTPException(413, "PDF 超過 30 MB · 請拆檔")

    started = datetime.utcnow()
    try:
        doc = fitz.open(stream=pdf_bytes, filetype="pdf")
    except Exception:
        raise HTTPException(400, "PDF 無法解析(可能加密或損毀)")

    pages = []
    ocr_count = 0
    for i, page in enumerate(doc):
        text = page.get_text("text").strip()
        ocr_used = False
        # 文字密度 threshold:< 120 字 + 有圖 = 偏掃描頁 · 走 OCR
        if len(text) < 120 and (page.get_images() or len(page.get_text("words")) < 30):
            try:
                tp = page.get_textpage_ocr(language="chi_tra+eng")
                text = page.get_text(textpage=tp).strip()
                ocr_used = True
                ocr_count += 1
            except Exception:
                pass  # OCR 失敗不擋 · 留原 text(可能空)
        pages.append({"page": i + 1, "text": text, "ocr_used": ocr_used,
                      "char_count": len(text)})

    combined = "\n\n".join([f"=== 第 {p['page']} 頁 ===\n{p['text']}" for p in pages])
    return {
        "stats": {
            "page_count": len(pages),
            "ocr_pages": ocr_count,
            "extract_seconds": (datetime.utcnow() - started).total_seconds(),
        },
        "pages": pages,
        "combined_text": combined,
    }
```

### 4. 失敗 / 降級
| 情境 | 處理 |
|---|---|
| 非 PDF | 400 + 「請上傳 PDF」 |
| > 30 MB | 413 + 「請拆檔」 |
| 加密 / 損毀 | 400 + 「無法解析」 |
| Tesseract 未安裝 | OCR 自動跳過 + stats 標 ocr_pages=0 |
| 全頁 OCR 失敗 | 仍回有抽到的頁 + friendly note「此 PDF 偏像掃描影本,先抽到可讀頁面,其餘請分段貼上」 |

### 5. Dockerfile 變動

```dockerfile
# backend/accounting/Dockerfile · 加 OCR 依賴
RUN apt-get update && apt-get install -y \
    tesseract-ocr tesseract-ocr-chi-tra \
    && rm -rf /var/lib/apt/lists/*
# requirements.txt 加:pymupdf==1.24.0
```

### 6. 前端 UI

`modules/chat.js` 的 `pickFile()` 改為真實開檔上傳:
```javascript
async pickFile() {
  const input = document.createElement("input");
  input.type = "file";
  input.accept = "application/pdf";
  input.onchange = async (e) => {
    const f = e.target.files[0];
    if (!f) return;
    toast.info(`抽取 ${f.name} · 大檔需 10-30 秒`);
    const fd = new FormData();
    fd.append("file", f);
    try {
      const r = await fetch("/api-accounting/pdf/extract", { method: "POST", body: fd });
      const d = await r.json();
      if (!r.ok) { toast.error(d.detail || "抽取失敗"); return; }
      const ta = document.getElementById("chat-input");
      ta.value = d.combined_text + "\n\n";
      ta.focus();
      toast.success(`${d.stats.page_count} 頁完成 · ${d.stats.ocr_pages} 頁用 OCR`);
    } catch (err) { toast.error("檔案抽取失敗 · 請改用複製貼上"); }
  };
  input.click();
}
```

附件按鈕(目前是 disabled)改成 enabled,但只接受 PDF。

### 7. 測試 / 驗收
- [ ] 1 份 born-digital 政府 PDF → ocr_pages=0 · 完整保留頁碼
- [ ] 1 份掃描 PDF → ocr_pages > 0 · 至少抽到部分頁
- [ ] 加密 PDF → 400
- [ ] 58 頁 PDF 在 15 秒內完成
- [ ] Tesseract 缺失時 · OCR pages 0 但純文字頁仍正常

### 8. 風險
- OCR 依賴(Tesseract)Docker image 大 30-50MB · 建議獨立小 image 或同 accounting
- pdfplumber 之後加 v1.2 · 處理表格抽取(annex 表格)

### 9. 工時:**8-12h**

---

## C · 跨助手 Handoff 4 格卡(v1.2 · 跨日跨人 artifact)

### 0. 現狀
- ✅ Projects collection 有 name/client/budget/deadline/description/status/owner(`main.py:548`)
- ✅ 前端 projects.js 有 CRUD `modules/projects.js:19`
- ❌ 缺 handoff 子物件 schema + UI

### 1. API 設計

```
PUT /api-accounting/projects/{id}/handoff
Body: {
  "goal": "中秋節社群活動主視覺",
  "constraints": ["品牌色橘黃", "預算 5 萬", "3 天內"],
  "asset_refs": [
    {"type": "nas", "label": "客戶舊包裝", "ref": "/Volumes/NAS/clients/XX/2024/"},
    {"type": "url", "label": "競品參考", "ref": "https://..."}
  ],
  "next_actions": ["設計師產 3 方向 · 週四前", "PM 排與客戶提案會議"],
  "source_conversation_id": "convo_xxx?",
  "updated_by": "alice@chengfu.local"
}

Response: { "ok": true, "updated_at": "..." }
```

**獨立 endpoint** · 不用 PUT /projects/{id} 全量更新(會洗掉其他欄位)

### 2. Python 實作

```python
class HandoffCard(BaseModel):
    goal: str = ""
    constraints: list[str] = []
    asset_refs: list[dict] = []
    next_actions: list[str] = []
    source_conversation_id: Optional[str] = None

@app.put("/projects/{project_id}/handoff")
def update_handoff(project_id: str, card: HandoffCard,
                   email: Optional[str] = Depends(current_user_email)):
    payload = {
        **card.model_dump(),
        "updated_by": email,
        "updated_at": datetime.utcnow(),
    }
    r = projects_col.update_one(
        {"_id": ObjectId(project_id)},
        {"$set": {"handoff": payload}}
    )
    if r.matched_count == 0:
        raise HTTPException(404, "專案不存在")
    return {"ok": True, "updated_at": payload["updated_at"].isoformat()}
```

### 3. UI 設計
- 位置:**Projects 詳情 drawer · 不放 chat pane**(reviewer 強調是跨日跨人 artifact)
- 4 格用短 textarea / 條列 chip
- **空白時預設收合** · 避免「又一張表單」感
- **「插入對話」按鈕** · 把 4 格轉為 prompt 開新對話:

```
你接到這個專案的交棒:
目標:[goal]
限制:[constraints 條列]
附件來源:[asset_refs 條列]
下一步:[next_actions 條列]

請先回覆:
1. 你理解了什麼?
2. 還缺什麼資訊?
3. 你打算怎麼開始?
```

### 4. 多分頁同步
透過已有的 `Projects` BroadcastChannel · PM 存卡後設計師另一分頁 1 秒內看到。

### 5. 失敗 / 降級
- 空卡不擋流程 · 不強迫必填
- `asset_refs` 的 NAS 路徑無法在 launcher 直接打開(NAS 整合是 v2)· 顯示成可複製文字

### 6. 測試 / 驗收
- [ ] PM 存一張卡 · 設計師另一分頁 1 秒內看到
- [ ] 點「插入對話」會把四格轉成 prompt 開新對話
- [ ] 空卡不擋流程

### 7. 風險
- **真正風險不是技術 · 是 adoption** — 4 格太像行政表單就不會有人填
- → 預設收合 + 一秒插入對話 + 不強迫必填

### 8. 工時:**6-8h**(放 v1.2 · 因為 adoption 需要 v1.1 上線後才能驗證)

---

## D · main.py 拆 services/admin_metrics.py(降低外包接手成本)

### 0. 現狀
最適合先拆的區塊集中在 `main.py:1030-1291` · 包含:
- `_ANTHROPIC_PRICING_USD` + `_PRICE_VERSION` 常數
- `_LC_TX_SCHEMA_CHECKED` schema cache
- `_lc_tx_probe()` / `_lc_tx_normalize()` adapter
- `_price_ntd()` / `_user_month_spend_ntd()` helpers
- `/admin/budget-status` · `/admin/top-users` · `/admin/tender-funnel` · `/admin/librechat-contract` · `/quota/check` 共用邏輯

### 1. 設計決策(reviewer 強調)
- **不用 APIRouter · 拆成 pure functions / small service class**
- **route decorator 全部留在 main.py** · 避免新 split-brain
- **FastAPI 註冊點仍只有 main.py 一個** · 這是底線

### 2. 新檔結構

```
backend/accounting/
├── main.py                  # route decorators · 1500 行(降 300+)
├── services/
│   ├── __init__.py
│   └── admin_metrics.py     # pure logic · ~280 行
├── test_main.py             # endpoint integration tests
└── tests/
    └── test_admin_metrics.py # service unit tests · 新加
```

### 3. `services/admin_metrics.py` 內容

```python
"""
Admin / ROI 計算 service · pure functions · 不直接定義 endpoint
main.py 的 /admin/* handler 呼叫這裡
"""
from datetime import datetime, timedelta
from typing import Optional
from bson import ObjectId

# ---- Pricing(可追溯)----
PRICE_VERSION = "2026-04-21"
PRICE_SOURCE = "https://www.anthropic.com/pricing"
ANTHROPIC_PRICING_USD = {
    "claude-opus-4-7":   {"input": 15.0,  "output": 75.0},
    "claude-sonnet-4-6": {"input":  3.0,  "output": 15.0},
    "claude-haiku-4-5":  {"input":  0.25, "output":  1.25},
}

# ---- Schema cache(test 前 reset 避免汙染)----
_SCHEMA_CACHED = {"checked": False, "ok": False, "issue": ""}

def reset_cache():
    """測試用 · 每 test 前呼叫"""
    _SCHEMA_CACHED.update({"checked": False, "ok": False, "issue": ""})

def probe_tx_schema(db) -> dict:
    if _SCHEMA_CACHED["checked"]:
        return _SCHEMA_CACHED
    _SCHEMA_CACHED["checked"] = True
    try:
        doc = db.transactions.find_one({}, {"rawAmount": 1, "model": 1, "user": 1, "createdAt": 1})
        if not doc:
            _SCHEMA_CACHED["ok"] = True
            _SCHEMA_CACHED["issue"] = "transactions 尚無資料(正常)"
        else:
            missing = []
            raw = doc.get("rawAmount") or {}
            if not isinstance(raw, dict): missing.append("rawAmount(non-dict)")
            elif "prompt" not in raw and "completion" not in raw:
                missing.append("rawAmount.prompt|completion")
            for k in ("model", "user", "createdAt"):
                if k not in doc: missing.append(k)
            _SCHEMA_CACHED["ok"] = not missing
            _SCHEMA_CACHED["issue"] = f"缺欄位:{missing}" if missing else ""
    except Exception as e:
        _SCHEMA_CACHED["ok"] = False
        _SCHEMA_CACHED["issue"] = f"probe 失敗:{e}"
    return _SCHEMA_CACHED


def price_ntd(model: str, tin: int, tout: int, usd_to_ntd: float = 32.5) -> float:
    p = ANTHROPIC_PRICING_USD.get(model) or ANTHROPIC_PRICING_USD["claude-sonnet-4-6"]
    usd = (tin / 1_000_000) * p["input"] + (tout / 1_000_000) * p["output"]
    return round(usd * usd_to_ntd, 2)


def user_month_spend_ntd(db, email: str, users_col, usd_to_ntd: float = 32.5) -> float:
    if not email: return 0.0
    u = users_col.find_one({"email": email}, {"_id": 1})
    if not u: return 0.0
    now = datetime.utcnow()
    month_start = now.replace(day=1, hour=0, minute=0, second=0, microsecond=0)
    pipeline = [
        {"$match": {"createdAt": {"$gte": month_start}, "user": u["_id"]}},
        {"$group": {"_id": "$model",
                    "tin":  {"$sum": "$rawAmount.prompt"},
                    "tout": {"$sum": "$rawAmount.completion"}}},
    ]
    stats = list(db.transactions.aggregate(pipeline))
    return sum(price_ntd(s["_id"] or "", s.get("tin", 0) or 0, s.get("tout", 0) or 0,
                         usd_to_ntd) for s in stats)


def budget_status(db, monthly_budget_ntd: float, usd_to_ntd: float = 32.5) -> dict:
    schema = probe_tx_schema(db)
    now = datetime.utcnow()
    month_start = now.replace(day=1, hour=0, minute=0, second=0, microsecond=0)
    pipeline = [
        {"$match": {"createdAt": {"$gte": month_start}}},
        {"$group": {"_id": "$model",
                    "tin":  {"$sum": "$rawAmount.prompt"},
                    "tout": {"$sum": "$rawAmount.completion"}}},
    ]
    try:
        stats = list(db.transactions.aggregate(pipeline))
        spent = sum(price_ntd(s["_id"] or "", s.get("tin", 0) or 0, s.get("tout", 0) or 0,
                              usd_to_ntd) for s in stats)
    except Exception:
        spent = 0.0
    pct = (spent / monthly_budget_ntd * 100) if monthly_budget_ntd else 0
    return {
        "spent_ntd": round(spent, 0),
        "budget_ntd": monthly_budget_ntd,
        "pct": round(pct, 1),
        "alert_level": "ok" if pct < 80 else "warn" if pct < 100 else "over",
        "month": now.strftime("%Y-%m"),
        "pricing_version": PRICE_VERSION,
        "data_source_ok": schema["ok"],
        "data_source_issue": schema["issue"] if not schema["ok"] else None,
    }


# ... top_users / tender_funnel / librechat_contract 同樣 pattern
```

### 4. `main.py` 變這樣

```python
from services import admin_metrics

@app.get("/admin/budget-status")
def budget_status_ep(_admin: str = Depends(require_admin)):
    return admin_metrics.budget_status(db, _MONTHLY_BUDGET_NTD, _USD_TO_NTD)

@app.get("/admin/top-users")
def top_users_ep(days: int = 30, limit: int = 10, _admin: str = Depends(require_admin)):
    return admin_metrics.top_users(db, _users_col, days, limit, _USER_SOFT_CAP_DEFAULT, _USD_TO_NTD)

@app.get("/admin/librechat-contract")
def librechat_contract_ep(_admin: str = Depends(require_admin)):
    return admin_metrics.librechat_contract(db)

@app.get("/quota/check")
def quota_check_ep(email: Optional[str] = Depends(current_user_email)):
    return admin_metrics.quota_check(db, _users_col, email,
                                       QUOTA_MODE, QUOTA_OVERRIDE_EMAILS,
                                       _admin_allowlist, _USER_SOFT_CAP_DEFAULT, _USD_TO_NTD)
```

main.py 從 1800+ 行 → 約 1520 行(減 280 行)

### 5. 測試策略

```python
# tests/test_admin_metrics.py(新加)
import pytest
import mongomock
from services import admin_metrics

@pytest.fixture(autouse=True)
def reset_cache():
    """每 test 前清 _SCHEMA_CACHED · 避免汙染"""
    admin_metrics.reset_cache()
    yield
    admin_metrics.reset_cache()

def test_price_ntd_haiku():
    cost = admin_metrics.price_ntd("claude-haiku-4-5", 1_000_000, 1_000_000)
    assert cost == round((0.25 + 1.25) * 32.5, 2)

def test_probe_tx_schema_empty(client_db):
    s = admin_metrics.probe_tx_schema(client_db)
    assert s["ok"] is True
    assert "尚無資料" in s["issue"]

# ... 5-8 支 unit tests
```

`test_main.py` endpoint integration 測試**全保留** · 18 pass 不變。

### 6. 失敗 / 降級
- 拆完後 main.py + admin_metrics.py 任一改動 · pytest 都會跑 · 降低風險
- import circular:`admin_metrics` 不 import main · 只接收 db / users_col / settings 參數

### 7. 驗收
- [ ] 18 pytest pass(原)
- [ ] 5 個新 unit tests pass(新)
- [ ] main.py 行數降 220-280
- [ ] 外包工程師看 `services/admin_metrics.py` 一檔就懂 ROI 邏輯

### 8. 風險
- 拆得太多會新 split-brain · **守底線:FastAPI 註冊點只有 main.py 一個**
- 若同時把 route 也搬走(用 APIRouter)就會分裂 · 不要這麼做

### 9. 工時:**6-8h**

---

## 共用建議

### 資源依賴(請承富老闆 / Sterio 確認後再動工)
- A:`FAL_API_KEY` 進 Keychain
- B:`accounting` Dockerfile 加 tesseract-ocr-chi-tra(image 大 30-50MB)+ requirements 加 pymupdf
- C:無新依賴
- D:無新依賴

### Round 8 reviewer 給作者的 5 題(寫進 `docs/QUESTIONS-FOR-OWNER.md`)
1. 承富常見招標 PDF · 掃描件 vs born-digital 比例?
2. 設計師偏好「1 張慢慢重生」or「3 張挑方向」?
3. 第一批 `knowledge-base/samples/` 能匿名提供哪 3 類?(建議書 / 設計 brief / 廠商比價信)
4. accounting 容器能加 PyMuPDF + Tesseract?還是想把 OCR 留在宿主機腳本?
5. 專案詳情 UI 偏好 modal / drawer / 卡片展開?(reviewer 傾向 drawer)

---

**這份 spec 是 v1.1 的施工藍圖 · 老闆答完 5 題、Mac mini 上架後就照著做。**
